Client >

first run command : `npm install `

for start : `npm start`

Server >

first command :  `npm install `

for start server : `nodemon`

.env example

PORT=8080
SESSION_SECRET=""
MONGO_URI="""
